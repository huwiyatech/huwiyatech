{
  "buildCommand": "npx prisma generate --schema=./prisma/schema.prod.prisma && npx prisma db push --schema=./prisma/schema.prod.prisma --skip-generate --accept-data-loss && next build",
  "framework": "nextjs"
}
